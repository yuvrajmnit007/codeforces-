#include<bits/stdc++.h>
using namespace std;
int main(){
    int t;
    cin>>t;
    while(t--){
        int n,k;
        cin>>n>>k;
        vector<int>arr(n);
        int sum=0;
        unordered_map<int,int>freq;
        for(int i=0;i<n;i++){
            cin>>arr[i];
            sum+=arr[i];
            freq[arr[i]]++;
        }
        for
    }
}