#include <bits/stdc++.h>
using namespace std;
#define int long long
signed main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int t=1;
    while (t--) {
        int n,k;
        cin>>n>>k;
        string st;
        cin>>st;
        while(k--){
            string p;
            cin>>p;
            if(p=='switch'){
                int l,r;
                cin>>l>>r;

            }else{
                
            }
        }
    }
    return 0;
}